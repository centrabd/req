# 🗺️ خارطة طريق تطوير المشروع (Project Roadmap)

هذا الملف يوثق مراحل التطوير المخطط لها لتحويل نموذج إثبات المفهوم (PoC) إلى منظومة إدارة طلبات تقسيط متكاملة واحترافية لـ "سنتر عبدالله".

---

## 🟢 المرحلة الأولى: الواجهة الأمامية المتقدمة (Multi-Step UI & Validation)
**الهدف:** بناء نموذج التسجيل الثماني الأقسام بتجربة مستخدم سلسة ومنطق تحقق صارم.
- [ ] تحويل `index.html` إلى نموذج متعدد الخطوات (Wizard) مع شريط تقدم (Progress Bar).
- [ ] إضافة ميزة "الحفظ التلقائي" (Auto-save to LocalStorage) لمنع فقدان البيانات عند تحديث الصفحة أو انقطاع الإنترنت.
- [ ] برمجة المنطق الشرطي (Conditional Logic):
  - إذا كانت الوظيفة "موظف" أو "معاش" ➔ إظهار ضامنين فقط.
  - إذا كانت "أعمال حرة"، "تاجر"، أو "صنايعي" ➔ إظهار 3 ضامنين.
- [ ] إضافة التحقق من صحة البيانات (Client-Side Validation):
  - التحقق من أن الرقم القومي 14 رقماً ويتبع الصيغة المصرية (Regex).
  - التحقق من أن سن الضامن بين 21 و 60 سنة بناءً على تاريخ الميلاد المدخل.
  - منع الانتقال للخطوة التالية إذا كانت الحقول المطلوبة (`*`) فارغة.

---

## 🟡 المرحلة الثانية: توسيع قاعدة البيانات والـ Backend (Database Scaling)
**الهدف:** تجهيز "العقل" و"الذاكرة" لاستقبال البيانات المعقدة والمنظمة.
- [ ] تحديث أعمدة Google Sheet لتشمل كل حقول النموذج الجديد (بيانات المشتري، بيانات الضامنين، التفاصيل المالية، روابط الملفات).
- [ ] تحديث كود `Requests.gs` (دالة `doPost`) لاستقبال كائن JSON كبير ومعقد يحتوي على جميع الأقسام.
- [ ] تحسين دالة `ensureSheetStructure` للتعامل مع الأعمدة الجديدة ديناميكياً وإضافتها تلقائياً إذا كانت مفقودة.
- [ ] إضافة تنسيق تلقائي للأرقام والتواريخ في Google Sheet لتسهيل القراءة على الإدارة.

---

## 🟠 المرحلة الثالثة: نظام رفع الملفات (File Upload System) 
**الهدف:** السماح للعميل برفع صور البطاقات وإثبات الدفع وحفظها بشكل آمن ومنظم.
- [ ] **في الواجهة الأمامية (Frontend):**
  - إضافة دالة لضغط الصور (Image Compression) باستخدام `Canvas API` قبل الرفع لتقليل الحجم وتجنب حدود Google Apps Script.
  - تحويل الصور المضغوطة إلى صيغة `Base64`.
- [ ] **في الخلفية (Backend):**
  - تحديث `doPost` لاستقبال بيانات `Base64` الخاصة بالملفات.
  - استخدام `DriveApp` لإنشاء مجلد فرعي لكل طلب (باسم الـ UUID الخاص بالطلب).
  - فك تشفير `Base64` وحفظ الصورة في المجلد، ثم ضبط الصلاحيات على "Anyone with link can view".
  - إرجاع روابط الملفات (URLs) وحفظها في الأعمدة المخصصة في Google Sheet.

---

## 🔴 المرحلة الرابعة: لوحة تحكم الأدمن الاحترافية والأتمتة (Admin Pro & Automation)
**الهدف:** تحويل `admin.html` إلى أداة عمل يومية فعالة وسريعة للإدارة.
- [ ] إضافة شريط بحث وفلترة متقدم (بحث بالاسم، الرقم القومي، أو رقم الهاتف).
- [ ] إضافة ميزة "معاينة الصور" (Image Preview Modal): عند الضغط على رابط الصورة في الجدول، تفتح في نافذة منبثقة بدلاً من الانتقال لصفحة جديدة.
- [ ] إضافة زر "تصدير إلى Excel/CSV" لأغراض الأرشفة والتحليل الخارجي.
- [ ] **(ميزة متقدمة):** دمج رابط WhatsApp تلقائي: عند تغيير الحالة إلى "مقبول" أو "مرفوض"، يظهر زر للمدير لفتح محادثة واتساب مع العميل برسالة جاهزة ومخصصة تحتوي على اسم العميل وحالة الطلب.

---

## ⚠️ قواعد ذهبية أثناء التنفيذ (Golden Rules)
1. **الاختبار التراكمي:** لا يتم الانتقال للمرحلة التالية إلا بعد اختبار المرحلة الحالية بالكامل بنجاح.
2. **النسخ الاحتياطي:** يجب الاحتفاظ بنسخة احتياطية من كود `Requests.gs` قبل أي تعديل هيكلي كبير.
3. **مراقبة الحدود (Quotas):** مراقبة حدود Google Apps Script (6 دقائق كحد أقصى لتنفيذ السكربت، وحوالي 20,000 طلب يومياً). ضغط الصور في المرحلة الثالثة هو الحل الأمثل لتجنب تجاوز حدود الوقت.

---
**👨‍💻 المطور:** [centrabd](https://github.com/centrabd)  
**📅 آخر تحديث للخطة:** 2026
